services:
  spider-worker:
    volumes:
      - type: "bind"
        source: "../../../build/rust-targets/release/libnn.so"
        target: "${SPIDER_WORKER_PACKAGE_DIR:?}/nn/libnn.so"
        read_only: true
